services:
  - type: web
    name: dashboard-diretoria-c
    env: node
    buildCommand: npm install && npm run build && cd server && npm install
    startCommand: cd server && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: UPLOAD_TOKEN
        value: sjcomercial
